spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5433/task-manager
    username: user
    password: password

  application.name: createTask
  sql:
    init:
      mode: always
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8082
server:
  port: 8081